#include <stdio.h>
#include <math.h>

int main () {
    /* typedef unsigned char byte;
    byte d = 0;
    short int si;
    int i;
    long int li;
    long long int lli;
    unsigned int ui;
    unsigned long int uli;
    unsigned long long int ulli;

    printf("unsigned char: %d short int :%d\nint: %d\nlong int: %d\nlong long int: %d\nunsigned int: %d\nunsigned long int: %d\nunsigned long long int: %d\n",d, sizeof(si), sizeof(i), sizeof(li), sizeof(lli), sizeof(ui), sizeof(uli),sizeof(ulli));
    printf("%d\n", pow(2,3)); */


    printf("Empates %d\n", 7/3);
    printf("%d\n", 7%3);
}